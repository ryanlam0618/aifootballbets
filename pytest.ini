[pytest]
# Default to unit tests (integration tests require network + external tooling).
addopts = -m "not integration"
markers =
    integration: mark test as integration test (deselected by default; run with: pytest -m integration)
    slow: mark test as slow (>30 seconds)
